<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Property" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/dark_gray</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

    </style>

    <style name="AppThemes" parent="Theme.Design.Light.NoActionBar">
        <item name="android:background">@color/snow_white</item>
    </style>

    <style name="LoginBanner" parent="Theme.Property">
        <item name="android:src">@drawable/login_banner</item>
    </style>

    <style name="LoginTextColor" parent="Theme.Property">
        <item name="android:textColor">@color/ice_deep</item>
    </style>

    <style name="ServerConfBanner" parent="Theme.Property">
        <item name="android:src">@drawable/serverconf_banner</item>
    </style>

</resources>